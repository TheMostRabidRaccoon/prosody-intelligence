# Prosody Intelligence — runtime dependencies.
# Whisper transcription and TTS both go through hosted APIs (OpenAI /
# ElevenLabs), so this runs headless on a Linux VM — no GPU, no local models.
# System dependency: ffmpeg (pydub decoding + compositor encoding).
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
numpy>=1.26.0
openai>=1.0.0
elevenlabs>=1.0.0
praat-parselmouth>=0.4.3
pydub>=0.25.1
matplotlib>=3.8.0
Pillow>=10.0.0
python-docx>=1.0.0
